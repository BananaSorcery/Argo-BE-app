apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-auth
data:
  db-password: {{ .Values.password | b64enc }}
  db-username: {{ .Values.username | b64enc }}
  db-endpoint: {{ .Values.endpoint | b64enc }}